This directory exists to prevent Next.js from treating `src/pages` as the Pages Router.
We are using the App Router located in the `app/` directory at the project root.
Files in `src/pages` correspond to the Feature-Sliced Design (FSD) 'Pages' layer.
